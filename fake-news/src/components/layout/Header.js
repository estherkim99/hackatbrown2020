import React, { Component } from 'react';

class Header extends Component {
    render() {
        return (
            <h1>header</h1>
        );
    }
}

const headerStyle = {
    background: '#B895AF',
    color: '#fff',
    textAlign: 'center',
    padding: '10px'
}

export default Header;
